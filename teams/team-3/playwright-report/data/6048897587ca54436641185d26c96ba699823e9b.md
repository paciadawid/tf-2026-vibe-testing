# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: fd-06-checkout.spec.ts >> FD-06 · Checkout >> FD-06 · place order is refused while a required field is empty
- Location: teams/team-3/tests/fd-06-checkout.spec.ts:90:7

# Error details

```
Error: no order with Full Name empty

expect(locator).toHaveCount(expected) failed

Locator:  getByRole('heading', { name: 'Order Confirmed!' })
Expected: 0
Received: 1
Timeout:  5000ms

Call log:
  - no order with Full Name empty getByRole('heading', { name: 'Order Confirmed!' }) with timeout 5000ms
  - waiting for getByRole('heading', { name: 'Order Confirmed!' })
    14 × locator resolved to 1 element
       - unexpected value "1"

```

```
Error: message for Full Name

expect(locator).toBeVisible() failed

Locator: getByText(/Full Name.*(required|needed)|(enter|provide|need).*Full Name/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - message for Full Name getByText(/Full Name.*(required|needed)|(enter|provide|need).*Full Name/i).first() with timeout 5000ms
  - waiting for getByText(/Full Name.*(required|needed)|(enter|provide|need).*Full Name/i).first()

```

```yaml
- region "Notifications (F8)":
  - list
- region "Notifications alt+T"
- navigation:
  - link "F Foodora":
    - /url: /
  - button "New York, NY":
    - img
    - text: New York, NY
  - link "Sign In":
    - /url: /auth
    - img
    - text: Sign In
  - button "Cart":
    - img
    - text: Cart
- img
- heading "Order Confirmed!" [level=2]
- paragraph: Your order has been placed successfully.
- paragraph: "Estimated delivery: 25-35 min"
- paragraph: "Order #FDR-S81DKX"
- paragraph: "Total: $17.44"
- button "Track My Order"
- button "Back to Home"
```

```
Error: no order with Street Address empty

expect(locator).toHaveCount(expected) failed

Locator:  getByRole('heading', { name: 'Order Confirmed!' })
Expected: 0
Received: 1
Timeout:  5000ms

Call log:
  - no order with Street Address empty getByRole('heading', { name: 'Order Confirmed!' }) with timeout 5000ms
  - waiting for getByRole('heading', { name: 'Order Confirmed!' })
    14 × locator resolved to 1 element
       - unexpected value "1"

```

```
Error: message for Street Address

expect(locator).toBeVisible() failed

Locator: getByText(/Street Address.*(required|needed)|(enter|provide|need).*Street Address/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - message for Street Address getByText(/Street Address.*(required|needed)|(enter|provide|need).*Street Address/i).first() with timeout 5000ms
  - waiting for getByText(/Street Address.*(required|needed)|(enter|provide|need).*Street Address/i).first()

```

```yaml
- region "Notifications (F8)":
  - list
- region "Notifications alt+T"
- navigation:
  - link "F Foodora":
    - /url: /
  - button "New York, NY":
    - img
    - text: New York, NY
  - link "Sign In":
    - /url: /auth
    - img
    - text: Sign In
  - button "Cart":
    - img
    - text: Cart
- img
- heading "Order Confirmed!" [level=2]
- paragraph: Your order has been placed successfully.
- paragraph: "Estimated delivery: 25-35 min"
- paragraph: "Order #FDR-LQCD4K"
- paragraph: "Total: $17.44"
- button "Track My Order"
- button "Back to Home"
```

```
Error: no order with City empty

expect(locator).toHaveCount(expected) failed

Locator:  getByRole('heading', { name: 'Order Confirmed!' })
Expected: 0
Received: 1
Timeout:  5000ms

Call log:
  - no order with City empty getByRole('heading', { name: 'Order Confirmed!' }) with timeout 5000ms
  - waiting for getByRole('heading', { name: 'Order Confirmed!' })
    14 × locator resolved to 1 element
       - unexpected value "1"

```

```
Error: message for City

expect(locator).toBeVisible() failed

Locator: getByText(/City.*(required|needed)|(enter|provide|need).*City/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - message for City getByText(/City.*(required|needed)|(enter|provide|need).*City/i).first() with timeout 5000ms
  - waiting for getByText(/City.*(required|needed)|(enter|provide|need).*City/i).first()

```

```yaml
- region "Notifications (F8)":
  - list
- region "Notifications alt+T"
- navigation:
  - link "F Foodora":
    - /url: /
  - button "New York, NY":
    - img
    - text: New York, NY
  - link "Sign In":
    - /url: /auth
    - img
    - text: Sign In
  - button "Cart":
    - img
    - text: Cart
- img
- heading "Order Confirmed!" [level=2]
- paragraph: Your order has been placed successfully.
- paragraph: "Estimated delivery: 25-35 min"
- paragraph: "Order #FDR-63ZIOK"
- paragraph: "Total: $17.44"
- button "Track My Order"
- button "Back to Home"
```

```
Error: no order with Phone Number empty

expect(locator).toHaveCount(expected) failed

Locator:  getByRole('heading', { name: 'Order Confirmed!' })
Expected: 0
Received: 1
Timeout:  5000ms

Call log:
  - no order with Phone Number empty getByRole('heading', { name: 'Order Confirmed!' }) with timeout 5000ms
  - waiting for getByRole('heading', { name: 'Order Confirmed!' })
    14 × locator resolved to 1 element
       - unexpected value "1"

```

```
Error: message for Phone Number

expect(locator).toBeVisible() failed

Locator: getByText(/Phone Number.*(required|needed)|(enter|provide|need).*Phone Number/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - message for Phone Number getByText(/Phone Number.*(required|needed)|(enter|provide|need).*Phone Number/i).first() with timeout 5000ms
  - waiting for getByText(/Phone Number.*(required|needed)|(enter|provide|need).*Phone Number/i).first()

```

```yaml
- region "Notifications (F8)":
  - list
- region "Notifications alt+T"
- navigation:
  - link "F Foodora":
    - /url: /
  - button "New York, NY":
    - img
    - text: New York, NY
  - link "Sign In":
    - /url: /auth
    - img
    - text: Sign In
  - button "Cart":
    - img
    - text: Cart
- img
- heading "Order Confirmed!" [level=2]
- paragraph: Your order has been placed successfully.
- paragraph: "Estimated delivery: 25-35 min"
- paragraph: "Order #FDR-D6689K"
- paragraph: "Total: $17.44"
- button "Track My Order"
- button "Back to Home"
```

# Test source

```ts
  24  |     },
  25  |     async ({ page }) => {
  26  |       const [restaurant] = await availableRestaurants(page)
  27  |       const [first, second] = await openMenu(page, restaurant)
  28  |       await quickAdd(page, first.name)
  29  |       await quickAdd(page, second.name)
  30  |       const dialog = await openCart(page)
  31  |       const cart: Record<string, number> = {}
  32  |       for (const label of ['Subtotal', 'Delivery Fee', 'Service Fee', 'Total']) {
  33  |         cart[label] = await rowAmount(dialog, label)
  34  |       }
  35  |       await closeCart(page)
  36  |       await goToCheckout(page)
  37  | 
  38  |       await expect(page.getByRole('heading', { name: 'Delivery Address' })).toBeVisible()
  39  |       for (const field of [...requiredFields, 'Apt / Suite']) {
  40  |         await expect(page.getByRole('textbox', { name: field, exact: true })).toBeVisible()
  41  |       }
  42  |       await expect(page.getByRole('textbox', { name: /Delivery Instructions/ })).toBeVisible()
  43  |       await expect(page.getByRole('heading', { name: 'Payment Method' })).toBeVisible()
  44  |       await expect(page.getByRole('radio')).toHaveCount(3)
  45  |       await expect(page.getByRole('heading', { name: 'Order Summary' })).toBeVisible()
  46  | 
  47  |       // The cart panel stays in the page behind checkout: read only the Order Summary block.
  48  |       const summary = page
  49  |         .locator('div')
  50  |         .filter({ has: page.getByRole('heading', { name: 'Order Summary' }) })
  51  |         .filter({ hasText: 'Subtotal' })
  52  |         .last()
  53  |       // Each dish was added once: the summary line shows 1x the dish at the price its menu gave.
  54  |       for (const dish of [first, second]) {
  55  |         await expect(summary.getByText(new RegExp(`^1x ${escapeRegExp(dish.name)}$`))).toBeVisible()
  56  |         expect(await rowAmount(summary, `1x ${dish.name}`), dish.name).toBe(dish.cents)
  57  |       }
  58  |       for (const label of Object.keys(cart)) {
  59  |         expect(await rowAmount(summary, label), label).toBe(cart[label])
  60  |       }
  61  |     },
  62  |   )
  63  | 
  64  |   test(
  65  |     'FD-06 · payment methods offered with card selected by default',
  66  |     {
  67  |       annotation: {
  68  |         type: 'spec',
  69  |         description: 'Payment method is one of **Credit / Debit Card** (selected by default), **Cash on Delivery** or **Apple Pay**.',
  70  |       },
  71  |     },
  72  |     async ({ page }) => {
  73  |       await oneDishInCart(page)
  74  |       await goToCheckout(page)
  75  | 
  76  |       const card = page.getByRole('radio', { name: /Credit \/ Debit Card/ })
  77  |       const cash = page.getByRole('radio', { name: /Cash on Delivery/ })
  78  |       const apple = page.getByRole('radio', { name: /Apple Pay/ })
  79  |       await expect(page.getByRole('radio')).toHaveCount(3)
  80  |       await expect(card).toBeChecked()
  81  |       await expect(cash).not.toBeChecked()
  82  |       await expect(apple).not.toBeChecked()
  83  | 
  84  |       await cash.check()
  85  |       await expect(cash).toBeChecked()
  86  |       await expect(card).not.toBeChecked()
  87  |     },
  88  |   )
  89  | 
  90  |   test(
  91  |     'FD-06 · place order is refused while a required field is empty',
  92  |     {
  93  |       annotation: [
  94  |         {
  95  |           type: 'spec',
  96  |           description: '**Place Order** only places the order when every required field is filled in. Otherwise no order is placed, and each missing field shows a message saying what is needed.',
  97  |         },
  98  |         {
  99  |           type: 'bug',
  100 |           description: 'The app places the order with a required field empty (with Full Name empty, even with the whole form empty) and shows no message for the missing field.',
  101 |         },
  102 |       ],
  103 |     },
  104 |     async ({ browser }) => {
  105 |       test.setTimeout(150_000)
  106 |       for (const missing of requiredFields) {
  107 |         const context = await browser.newContext()
  108 |         const page = await context.newPage()
  109 |         await oneDishInCart(page)
  110 |         await goToCheckout(page)
  111 |         await fillRequired(page, missing)
  112 |         await page.getByRole('button', { name: 'Place Order' }).click()
  113 | 
  114 |         const field = escapeRegExp(missing)
  115 |         // A message that names the missing field and says it is needed.
  116 |         const message = page.getByText(
  117 |           new RegExp(`${field}.*(required|needed)|(enter|provide|need).*${field}`, 'i'),
  118 |         )
  119 |         const confirmed = page.getByRole('heading', { name: 'Order Confirmed!' })
  120 |         // Wait until the app has answered the click one way or the other.
  121 |         await expect(message.or(confirmed).first()).toBeVisible()
  122 |         await expect.soft(confirmed, `no order with ${missing} empty`).toHaveCount(0)
  123 |         await expect.soft(page, `still on checkout with ${missing} empty`).toHaveURL(/\/checkout$/)
> 124 |         await expect.soft(message.first(), `message for ${missing}`).toBeVisible()
      |                                                                      ^ Error: message for Phone Number
  125 |         await context.close()
  126 |       }
  127 |     },
  128 |   )
  129 | 
  130 |   test(
  131 |     'FD-06 · order places with optional fields blank',
  132 |     {
  133 |       annotation: {
  134 |         type: 'spec',
  135 |         description: '**Place Order** only places the order when every required field is filled in.',
  136 |       },
  137 |     },
  138 |     async ({ page }) => {
  139 |       await oneDishInCart(page)
  140 |       await goToCheckout(page)
  141 |       await fillRequired(page)
  142 |       await expect(page.getByRole('textbox', { name: 'Apt / Suite', exact: true })).toHaveValue('')
  143 |       await expect(page.getByRole('textbox', { name: /Delivery Instructions/ })).toHaveValue('')
  144 |       await page.getByRole('button', { name: 'Place Order' }).click()
  145 |       await expect(page.getByRole('heading', { name: 'Order Confirmed!', exact: true })).toBeVisible()
  146 |     },
  147 |   )
  148 | 
  149 |   test(
  150 |     'FD-06 · checkout with an empty cart shows an empty state',
  151 |     {
  152 |       annotation: {
  153 |         type: 'spec',
  154 |         description: 'Opening checkout with an empty cart shows an empty state with a way back to the restaurants — never a form that could place an empty order.',
  155 |       },
  156 |     },
  157 |     async ({ page }) => {
  158 |       await page.goto('/checkout', { waitUntil: 'domcontentloaded' })
  159 |       await expect(page.getByText(/empty/i).first()).toBeVisible()
  160 |       await expect(page.getByRole('button', { name: 'Place Order' })).toHaveCount(0)
  161 |       await expect(page.getByRole('textbox', { name: 'Full Name' })).toHaveCount(0)
  162 | 
  163 |       await page.getByRole('button', { name: /restaurants/i }).click()
  164 |       await expect(page.getByRole('heading', { name: 'Popular Restaurants' })).toBeVisible()
  165 |     },
  166 |   )
  167 | })
  168 | 
```