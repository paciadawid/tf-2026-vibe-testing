# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: fd-03-restaurant-menu.spec.ts >> FD-03 · Restaurant menu >> FD-03 · every button on the restaurant page has an accessible name
- Location: teams/team-3/tests/fd-03-restaurant-menu.spec.ts:136:7

# Error details

```
Error: expect(locator).toHaveAccessibleName(expected) failed

Locator: getByRole('button').nth(2)
Expected pattern: /\S/
Received string:  ""
Timeout: 5000ms

Call log:
  - Expect "soft toHaveAccessibleName" getByRole('button').nth(2) with timeout 5000ms
  - waiting for getByRole('button').nth(2)
    14 × locator resolved to <button class="inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 text-primary-foreground hover:bg-primary/90 bg-gradient-primary hover:opacity-90 border-0 rounded-full w-8 h-8 p-0">…</button>
       - unexpected value ""

```

```yaml
- button:
  - img
```

```
Error: expect(locator).toHaveAccessibleName(expected) failed

Locator: getByRole('button').nth(3)
Expected pattern: /\S/
Received string:  ""
Timeout: 5000ms

Call log:
  - Expect "soft toHaveAccessibleName" getByRole('button').nth(3) with timeout 5000ms
  - waiting for getByRole('button').nth(3)
    14 × locator resolved to <button class="inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 text-primary-foreground hover:bg-primary/90 bg-gradient-primary hover:opacity-90 border-0 rounded-full w-8 h-8 p-0">…</button>
       - unexpected value ""

```

```yaml
- button:
  - img
```

```
Error: expect(locator).toHaveAccessibleName(expected) failed

Locator: getByRole('button').nth(4)
Expected pattern: /\S/
Received string:  ""
Timeout: 5000ms

Call log:
  - Expect "soft toHaveAccessibleName" getByRole('button').nth(4) with timeout 5000ms
  - waiting for getByRole('button').nth(4)
    14 × locator resolved to <button class="inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 text-primary-foreground hover:bg-primary/90 bg-gradient-primary hover:opacity-90 border-0 rounded-full w-8 h-8 p-0">…</button>
       - unexpected value ""

```

```yaml
- button:
  - img
```

```
Error: expect(locator).toHaveAccessibleName(expected) failed

Locator: getByRole('button').nth(5)
Expected pattern: /\S/
Received string:  ""
Timeout: 5000ms

Call log:
  - Expect "soft toHaveAccessibleName" getByRole('button').nth(5) with timeout 5000ms
  - waiting for getByRole('button').nth(5)
    14 × locator resolved to <button class="inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 text-primary-foreground hover:bg-primary/90 bg-gradient-primary hover:opacity-90 border-0 rounded-full w-8 h-8 p-0">…</button>
       - unexpected value ""

```

```yaml
- button:
  - img
```

```
Test timeout of 30000ms exceeded.
```

```
Error: quick-add names its action

expect(locator).toHaveAccessibleName(expected) failed

Locator: getByRole('tabpanel').getByRole('link').first().getByRole('button')
Expected pattern: /add/i
Received string:  ""

Call log:
  - quick-add names its action getByRole('tabpanel').getByRole('link').first().getByRole('button') with timeout 5000ms
  - waiting for getByRole('tabpanel').getByRole('link').first().getByRole('button')
    13 × locator resolved to <button class="inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 text-primary-foreground hover:bg-primary/90 bg-gradient-primary hover:opacity-90 border-0 rounded-full w-8 h-8 p-0">…</button>
       - unexpected value ""
  - Test ended.

```

```yaml
- button:
  - img
```

```
Error: quick-add names its action

expect(locator).toHaveAccessibleName(expected) failed

Locator: getByRole('tabpanel').getByRole('link').nth(1).getByRole('button')
Expected pattern: /add/i
Received string:  ""

```

# Test source

```ts
  61  |     'FD-03 · each dish shows name, description, price and a quick-add button',
  62  |     {
  63  |       annotation: {
  64  |         type: 'spec',
  65  |         description: 'for each dish: name, short description and price, and a quick-add **+** button.',
  66  |       },
  67  |     },
  68  |     async ({ page }) => {
  69  |       await openRestaurant(page)
  70  |       const tabs = await page.getByRole('tablist').getByRole('tab').all()
  71  |       expect(tabs.length).toBeGreaterThan(0)
  72  |       for (const tab of tabs) {
  73  |         await tab.click()
  74  |         const dishes = await dishCards(page).all()
  75  |         expect(dishes.length, `no dishes in tab ${await tab.textContent()}`).toBeGreaterThan(0)
  76  |         for (const dish of dishes) {
  77  |           await expect(dish.getByRole('heading')).toHaveText(/\S/)
  78  |           await expect(dish.getByRole('paragraph').first()).toHaveText(/\S/)
  79  |           await expect(dish).toContainText(/\$\d+\.\d{2}/)
  80  |           await expect(dish.getByRole('button')).toHaveCount(1)
  81  |           await expect(dish.getByRole('button')).toBeVisible()
  82  |         }
  83  |       }
  84  |     },
  85  |   )
  86  | 
  87  |   test(
  88  |     'FD-03 · quick-add puts one dish in the cart, confirms it and raises the cart count by one',
  89  |     {
  90  |       annotation: {
  91  |         type: 'spec',
  92  |         description:
  93  |           'Quick-add puts one of that dish into the cart and confirms it. The cart count in the header goes up by one.',
  94  |       },
  95  |     },
  96  |     async ({ page }) => {
  97  |       await openRestaurant(page)
  98  |       const dish = dishCards(page).first()
  99  |       const name = (await dish.getByRole('heading').textContent())!.trim()
  100 |       const before = await cartCount(page)
  101 |       const url = page.url()
  102 | 
  103 |       // The quick-add button has no accessible name; it is the only button in the dish card.
  104 |       await dish.getByRole('button').click()
  105 | 
  106 |       await expect(page.getByRole('region', { name: /Notifications/ }).first()).toContainText(name)
  107 |       await expect.poll(() => cartCount(page)).toBe(before + 1)
  108 |       expect(page.url()).toBe(url)
  109 | 
  110 |       await page.getByRole('button', { name: /^Cart\b/ }).click()
  111 |       const cart = page.getByRole('dialog')
  112 |       await expect(cart.getByRole('heading', { name, exact: true })).toHaveCount(1)
  113 |     },
  114 |   )
  115 | 
  116 |   test(
  117 |     'FD-03 · selecting a dish opens its detail page',
  118 |     {
  119 |       annotation: {
  120 |         type: 'spec',
  121 |         description: 'Selecting the dish itself opens its detail page (FD-04).',
  122 |       },
  123 |     },
  124 |     async ({ page }) => {
  125 |       await openRestaurant(page)
  126 |       const dish = dishCards(page).first()
  127 |       const name = (await dish.getByRole('heading').textContent())!.trim()
  128 | 
  129 |       await dish.getByRole('heading').click()
  130 | 
  131 |       await expect(page).toHaveURL(/\/product\/[^/]+$/)
  132 |       await expect(page.getByRole('heading', { name, level: 1 })).toBeVisible()
  133 |     },
  134 |   )
  135 | 
  136 |   test(
  137 |     'FD-03 · every button on the restaurant page has an accessible name',
  138 |     {
  139 |       annotation: [
  140 |         {
  141 |           type: 'bug',
  142 |           description: 'Every quick-add button on the restaurant page is an icon-only button with no accessible name.',
  143 |         },
  144 |         {
  145 |           type: 'spec',
  146 |           description:
  147 |             'Every button can be used with a screen reader: each one has an accessible name that says what it does, including icon-only buttons such as quick-add.',
  148 |         },
  149 |       ],
  150 |     },
  151 |     async ({ page }) => {
  152 |       await openRestaurant(page)
  153 |       await expect(dishCards(page).first()).toBeVisible()
  154 |       const buttons = await page.getByRole('button').all()
  155 |       expect(buttons.length).toBeGreaterThan(0)
  156 |       for (const button of buttons) {
  157 |         await expect.soft(button).toHaveAccessibleName(/\S/)
  158 |       }
  159 |       // Quick-add says what it does: its name says it adds.
  160 |       for (const card of await dishCards(page).all()) {
> 161 |         await expect.soft(card.getByRole('button'), 'quick-add names its action').toHaveAccessibleName(/add/i)
      |                                                                                   ^ Error: quick-add names its action
  162 |       }
  163 |     },
  164 |   )
  165 | })
  166 | 
```